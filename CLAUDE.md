# CLAUDE.md — portfolio

Som Tripathi's portfolio site: somtripathi.dev (Vercel, auto-deploys from main on github.com/somtri/portfolio). Next.js 16 App Router, static pages + exactly one serverless route (/api/ask — the grounded assistant). Runtime deps are next/react/react-dom only, by design.

- Read DECISIONS.md in full before proposing any approach; read STATE.md for current position. Session handoffs: HANDOFF-2026-08-08.md (latest session) and handoff.md (owner's original — never commit, move, or delete either; note Windows treats HANDOFF.md and handoff.md as the same path, so dated names are mandatory).
- Content lives in data\*.ts (typed, imported by pages AND the assistant corpus). After content edits: run pnpm embed (reads .env.local via Node's own --env-file-if-exists; nothing here loads dotenv) and COMMIT both lib\assistant\vectors.json and vectors.meta.json — the runtime reads the vectors from disk on Vercel, so they must be in the repo, and a test compares the recorded corpus hash against the live corpus. Keep tests\ green — they guard honest-results wording (SmartSignal simulation caveat, Cine ML post-release framing, MacroMarkets negative result).
- Design system: v2 (cobalt-on-cream) is built on the branch but was REJECTED at owner review 2026-08-09. v3 is LOCKED (D-010/D-011/D-012): linux-session terminal — dark-native pure black, EVERYTHING lowercase, Hack for all text + Space Grotesk 700 only on the wordmark and connect button, accent #2F6BFF on dark / #0B76CC on light, coordinate annotations, anchored block cursor, morphing ask widget, weekly auto-rotating $ fortune line. The design contract is the comp pair at .claude\orchestration\design\v3-comps\b3-hack-adj.html and b3-hack-adj-light.html — authoritative over any doc prose, but HOME-ONLY: the comps never covered subpages. The v3 rollout is COMPLETE as of 4a916ab; every subpage follows the register derived from app\page.tsx and written down in .claude\orchestration\briefs\038-040 — a `$ command` heading, `[ bracketed ]` faint annotations, ruled rows, no display titles, no cards, no rounded corners. Reuse those briefs for any new page rather than re-deriving. Build pre-authorized; semantic tokens only in app\globals.css still applies; fonts self-host as static woff2 (no build-time font fetch); the tab icon follows the SITE theme toggle (D-017/D-019) — components\TabIcon.tsx creates both icon links itself from public\icon-{dark,light}.{svg,png}, because Next's file-convention icon is React-rendered and rewriting it races hydration; public\favicon.ico ships but is deliberately never declared as a link.
- Assistant (lib\assistant\): RAG-lite per D-003 — no vector DB, no LangChain. Providers per D-004: Groq chat + Cloudflare embeddings via env-configured OpenAI-compatible fetch adapters; NVIDIA is dev/eval only (its trial ToS bars free production use). Never let a key reach the browser; .env.example holds names only.
- Verification bar: pnpm lint, typecheck, test, build — all green before claiming done. All four pass offline with no env vars (fallback paths guarantee it).
- Git: never push or deploy without the owner's say-so; main auto-deploys to production. Never use Vercel CLI to deploy.
